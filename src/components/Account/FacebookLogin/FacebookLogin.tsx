import React from "react"
import { Button } from "react-bootstrap"

export default function FacebookLogin () {

  function handleFacebook () {
    
  }
  
  return (
    <div>
      <Button variant="primary" onClick={handleFacebook}>
        Login in with Facebook
      </Button>
    </div>
  )
}